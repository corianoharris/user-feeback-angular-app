export interface UserData
{
    id: string;
    name: string;
    email: string;
    message: string;
    createdAt: string;
}

export interface UserFormData
{
    name: string;
    email: string;
    message: string;
}